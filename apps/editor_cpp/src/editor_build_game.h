#pragma once

namespace dse::editor {

/// Draw the "Build Game" modal dialog (call every frame; shows only when open)
void DrawBuildGameDialog();

/// Open the Build Game dialog
void OpenBuildGameDialog();

} // namespace dse::editor
