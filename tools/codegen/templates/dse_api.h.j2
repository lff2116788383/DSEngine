/**
 * @file dse_api.gen.h
 * @brief 自动生成 — 勿手动修改
 *        来源：tools/codegen/binding_defs.json
 *
 * 由 dse_api.h 在末尾 #include 本文件，为 codegen 生成的组件字段
 * 提供 C ABI 声明。使用独立 include guard，可安全与 dse_api.h 共存。
 */

#ifndef DSE_API_GEN_H
#define DSE_API_GEN_H

#include <stdint.h>

#ifndef DSE_CAPI
#ifdef _WIN32
#  define DSE_CAPI __declspec(dllexport)
#else
#  define DSE_CAPI __attribute__((visibility("default")))
#endif
#endif

#ifdef __cplusplus
extern "C" {
#endif

{% for comp in components %}
/* ---- {{ comp.name }} ---- */
{% for field in comp.fields %}
{% if field.type == "float" %}
DSE_CAPI float dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e);
{% if not field.get("readonly", false) %}
DSE_CAPI void  dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, float v);
{% endif %}
{% elif field.type in ("int", "enum_int") %}
DSE_CAPI int  dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e);
{% if not field.get("readonly", false) %}
DSE_CAPI void dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, int v);
{% endif %}
{% elif field.type == "bool" %}
DSE_CAPI int  dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e);
{% if not field.get("readonly", false) %}
DSE_CAPI void dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, int v);
{% endif %}
{% elif field.type == "vec2" %}
DSE_CAPI void dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e, float* x, float* y);
{% if not field.get("readonly", false) %}
DSE_CAPI void dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, float x, float y);
{% endif %}
{% elif field.type in ("vec3", "euler_quat") %}
DSE_CAPI void dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e, float* x, float* y, float* z);
{% if not field.get("readonly", false) %}
DSE_CAPI void dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, float x, float y, float z);
{% endif %}
{% elif field.type == "vec4" %}
DSE_CAPI void dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e, float* x, float* y, float* z, float* w);
{% if not field.get("readonly", false) %}
DSE_CAPI void dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, float x, float y, float z, float w);
{% endif %}
{% elif field.type == "string" %}
{% if field.get("capi_setter") != "manual" %}
DSE_CAPI void dse_{{ comp.prefix }}_set_{{ field.name }}(uint32_t e, const char* v);
{% endif %}
DSE_CAPI int  dse_{{ comp.prefix }}_get_{{ field.name }}(uint32_t e, char* buf, int buf_size);
{% endif %}
{% endfor %}

{% endfor %}
#ifdef __cplusplus
}
#endif

#endif // DSE_API_GEN_H
