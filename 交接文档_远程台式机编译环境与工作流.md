# 交接文档：远程连接台式机 + 跨机传文件 + 台式机编译测试

> 生成时间：2026-09-15（本轮会话）
> 用途：供后续 AI/开发者快速掌握「如何从本机（笔记本）远程连到台式机、如何把本机文件传到台式机、如何在台式机的 WSL 里编译并测试本项目」
> 覆盖：远程连接 / 文件传输 / WSL 环境 / 编译测试 / LLVM 修复 / 本轮成果

---

## ⚠️ 一句话速览

- 笔记本通过 **SSH** 连台式机（`Administrator@169.254.139.190`，网线内网、**无外网**、Windows 10 加域）。
- 台式机上用 **WSL2（新版 Store 版 2.7.14）** 里的 Ubuntu2204 + conda `plcbuild` 环境编译本项目。
- 传到台式机的文件**统一先 base64 编码成 `.b64` 再 scp**，到台式机后用 WSL 内 `base64 -d` 还原执行 —— 这是本轮验证过**最稳**的方式（规避 SSH/PowerShell 的各种转义陷阱）。
- 编译/测试耗时极短（全流程约 66s），可放心用台式机跑。

---

## 一、远程连接台式机

### 1.1 基础参数

| 项 | 值 |
|----|----|
| 台式机 IP | `169.254.139.190`（网线直连，内网） |
| SSH 用户 | `Administrator` |
| 系统 | Windows 10，加域，**无外网** |
| 外网 | 不可用（apt/conda 联网都会失败） |

### 1.2 SSH 常用命令模板

```powershell
# 在台式机的 Windows PowerShell 里执行命令
ssh -o BatchMode=yes -o StrictHostKeyChecking=no Administrator@169.254.139.190 "powershell -NoProfile -File C:\ProgramData\xxx.ps1"

# 在台式机的 WSL 里执行命令（前台同步，保持 VM 存活）
ssh -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=600 Administrator@169.254.139.190 "powershell -NoProfile -ExecutionPolicy Bypass -File C:\ProgramData\xxx.ps1"
```

- `-o ServerAliveInterval=30`：**长时间编译/测试必加**，防止 SSH 因无数据空闲被切断。

### 1.3 新版 WSL2 的调用方式（重要！）

- 台式机已升级到 **Store 版 WSL 2.7.14**（修复了旧版 LxssManager 死锁导致的**卡死/超时**问题）。
- **新版 `wsl.exe` 在 `C:\Program Files\WSL\wsl.exe`**，不在 `C:\Windows\System32\wsl.exe`。
- 发行版名：`Ubuntu2204`。
- 在台式机 PowerShell 里执行 WSL 命令用：

```powershell
$wsl = 'C:\Program Files\WSL\wsl.exe'
& $wsl -d Ubuntu2204 bash -lc "要执行的bash命令"
```

- ⚠️ **潜在干扰**：新版 WSL 每次调用可能打印 `wsl: Failed to start the systemd user session for 'root'` 的警告，**不影响执行**，忽略即可。
- 验证版本：`wsl --version` 应显示 `2.7.14`（秒回，不再卡死）。

---

## 二、从本机传文件到台式机（核心工作流）

### 2.1 为什么必须用 base64 中转

SSH 用双引号套命令时，**PowerShell 会展开 `$变量`，bash 的 `|`、`>`、复杂引号会被吞掉/断义**，导致脚本损坏。包裹整个脚本为 base64 可彻底规避，也是本轮验证的**标准做法**。

### 2.2 标准五步流程（bash 脚本为例）

**第 1 步：本地写脚本**（如 `_myjob.sh`，在本机任何能放的地方）。

**第 2 步：本地 base64 编码 + scp 到台式机**

```powershell
$c = [System.IO.File]::ReadAllText("D:\Share\plc-inauto-compiler\_myjob.sh")
$b = [Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($c))
[System.IO.File]::WriteAllText("D:\Share\plc-inauto-compiler\_myjob.sh.b64", $b)
scp -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=20 `
  "D:\Share\plc-inauto-compiler\_myjob.sh.b64" `
  Administrator@169.254.139.190:"C:/ProgramData/_myjob.sh.b64"
```

- ⚠️ scp 目标路径**必须用 Windows 格式** `C:/ProgramData/...`，**不要**用 Git Bash 的 `/c/...`。

**第 3 步：本地写一个 runner**（`_run_myjob.ps1`，负责在台式机 WSL 里解码并执行）：

```powershell
$wsl = 'C:\Program Files\WSL\wsl.exe'
$inner = 'base64 -d /mnt/c/ProgramData/_myjob.sh.b64 > /tmp/_myjob.sh 2>/dev/null && bash /tmp/_myjob.sh 2>&1 | tee /mnt/c/ProgramData/result.log'
& $wsl -d Ubuntu2204 bash --noprofile --norc -e -c "$inner"
```

**第 4 步：scp 上传 runner**

```powershell
scp -o BatchMode=yes -o ConnectTimeout=20 `
  "D:\Share\plc-inauto-compiler\_run_myjob.ps1" `
  Administrator@169.254.139.190:"C:/ProgramData/_run_myjob.ps1"
```

**第 5 步：SSH 执行 runner（前台同步）**

```powershell
ssh -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=600 `
  Administrator@169.254.139.190 `
  "powershell -NoProfile -ExecutionPolicy Bypass -File C:\ProgramData\_run_myjob.ps1"
```

> 结果可落盘到台式机 Windows 侧 `C:/ProgramData/result.log` 再拉回分析。

### 2.3 传小文件 / 单个文本

- 通用做法：本地 base64 → scp `.b64` → 台式机 WSL `base64 -d > 目标路径`。
- 不要直接 scp 原始 .sh/.ps1（Windows/WSL 间可能有 CRLF/编码问题，base64 最稳）。

### 2.4 从台式机拉文件回本机

```powershell
scp Administrator@169.254.139.190:"C:/ProgramData/result.log" "D:\Share\plc-inauto-compiler\result.log"
```

---

## 三、台式机编译环境

### 3.1 源码与构建目录

| 项 | 路径 |
|----|------|
| 源码（WSL） | `/opt/plc2/plc-inauto-compiler` |
| 构建目录 | `/opt/plc2/plc-inauto-compiler/build_clean` |
| 编译器产物 | `build_clean/bin/plc-compiler`、`integration_tests` 等 |

### 3.2 编译器与工具

- **GCC 11**（`/usr/bin/g++-11`、`/usr/bin/gcc-11`）—— 能编译本项目最新源码，无需 GCC 14。
- **Ninja**：`/usr/local/bin/ninja`；**CMake** 在 conda 里。
- **LLVM/MLIR 18.1.3**：conda `plcbuild`（`/opt/miniconda3/envs/plcbuild`）。

### 3.3 conda 环境 `plcbuild`

- 路径：`/opt/miniconda3/envs/plcbuild`
- 关键依赖：LLVM/MLIR 18.1.3、fmt 10、spdlog、antlr4。
- `find_package` 用到的 cmake 目录：
  - `LLVM_DIR=/opt/miniconda3/envs/plcbuild/lib/cmake/llvm`
  - `MLIR_DIR=/opt/miniconda3/envs/plcbuild/lib/cmake/mlir`
- 运行时需 `LD_LIBRARY_PATH=/opt/miniconda3/envs/plcbuild/lib`。

### 3.4 重新 configure 的标准命令（已验证）

在 `build_clean` 里执行（脚本内先 `source /opt/miniconda3/etc/profile.d/conda.sh && conda activate plcbuild`）：

```bash
cmake -G Ninja \
  -DCMAKE_CXX_COMPILER=/usr/bin/g++-11 \
  -DCMAKE_C_COMPILER=/usr/bin/gcc-11 \
  -DCMAKE_MAKE_PROGRAM=/usr/local/bin/ninja \
  -DCMAKE_BUILD_TYPE=Release \
  -DLLVM_DIR=/opt/miniconda3/envs/plcbuild/lib/cmake/llvm \
  -DMLIR_DIR=/opt/miniconda3/envs/plcbuild/lib/cmake/mlir \
  /opt/plc2/plc-inauto-compiler
```

---

## 四、编译与测试

### 4.1 快速验证单文件（CommandLine 是否正常的红灯信号）

```bash
BUILD=/opt/plc2/plc-inauto-compiler/build_clean
printf 'PROGRAM hello;\nEND_PROGRAM\n' > /tmp/h.st
timeout 30 $BUILD/bin/plc-compiler /tmp/h.st -o /tmp/hout --emit-ir
# 期望：Compilation successful! / LLVM IR: /tmp/hout/h.ll  ，退出码 0
```

- 若出现 `Option '...' registered more than once` 或 `LLVM ERROR: inconsistency in registered CommandLine options` → 说明 LLVM 链接带回双源（见第六节）。

### 4.2 构建并运行集成测试（完整回归）

```bash
BUILD=/opt/plc2/plc-inauto-compiler/build_clean
/usr/local/bin/ninja integration_tests     # 含 gtest_discover 枚举，稍有 CommandLine 问题就会在这崩溃
timeout 300 $BUILD/bin/integration_tests
```

- 本轮基线：**Running 1767 tests / 116 套件，PASSED 1581、SKIPPED 373、FAILED 0（约 4.9s）**。
- 其它测试可执行程序在 `build_clean/bin/`：`ast_unit_tests`、`semantic_unit_tests`、`mlir_st_dialect_tests`。

### 4.3 从笔记本同步源码到台式机

- 台式机无外网，源码靠本机打包传输：本地把**干净源码子集**（排除 build、.git、output 等）打成 `tar.gz`，base64 中转或直接 scp 到 `C:/ProgramData/`，再在 WSL 解到 `/opt/plc2/plc-inauto-compiler`。
- 解压命令（WSL）：`tar -xzf /mnt/c/ProgramData/_src_clean.tar.gz -C /opt/plc2/`。

---

## 五、LLVM CommandLine 重复注册修复（本轮核心，已解决）

### 5.1 现象与根因

- **现象**：`plc-compiler` 编译成功，运行时 `CommandLine Error: Option '...' registered more than once` 崩溃；`integration_tests` 在 gtest 枚举阶段就 build 失败。
- **根因**：conda `plcbuild` 的 LLVM **同时**提供静态组件 `.a` 与共享库 `libLLVM.so.18.1`。
  - 项目 `llvm_map_components_to_libnames` 返回**静态组件** `.a`（`PLC_LLVM_LIBS`）；
  - MLIR target 在 `LLVM_LINK_LLVM_DYLIB=yes` 下**自动引入 `libLLVM.so`**；
  - 于是同一批 `cl::Option` 符号被实例化两次。

### 5.2 修复（改环境，不改项目源码）

修改台式机 conda 的 `LLVM-Config.cmake`（在 `llvm_map_components_to_libnames` 函数开头注入）：

```cmake
# CatPaw envfix: 当 LLVM_LINK_LLVM_DYLIB=yes 时只返回单一 LLVM dylib
if(LLVM_LINK_LLVM_DYLIB AND TARGET LLVM)
  set(${out_libs} LLVM PARENT_SCOPE)
  return()
endif()
```

- 文件：`/opt/miniconda3/envs/plcbuild/lib/cmake/llvm/LLVM-Config.cmake`
- ⚠️ **已备份**：`LLVM-Config.cmake.bak.envfix`（回滚：`cp LLVM-Config.cmake.bak.envfix LLVM-Config.cmake`，再重新 configure）。
- 改完**必须重新 configure**（`build_clean` 里按 3.4 执行）才会生效。
- 验证：configure 输出应为 `LLVM libraries: LLVM`（单一 dylib，而非一堆静态组件）。

### 5.3 修复前必须先满足的环境前提

- **fmt 必须为 10**（与 spdlog ABI 匹配），删除任何 fmt12 残留，避免 `undefined reference to fmt::v10::vformat`。

---

## 六、关键经验教训（本轮踩坑汇总）

1. **新版 WSL 路径变了**：`wsl.exe` 在 `C:\Program Files\WSL\`；用旧版命令或路径可能导致找不到。
2. **PowerShell 里混写 bash 语法必炸**：在 `.ps1` runner 里不要再写 `rm`、`tail`、`/mnt/c/...` 等 bash 语句 —— 必须把 Linux 语句放进 `& $wsl bash -lc "..."` 里执行。
3. **SSH 会话结束会杀 WSL 后台进程**：长编译/测试要**前台同步**跑（SSH `-o ServerAliveInterval=30` 保活），不要依赖 SSH 里 `nohup &`。
4. **传文件一律 base64 中转**：规避 PowerShell 变量展开、bash 管道引号吞字。
5. **scp 目标路径用 Windows 格式** `C:/ProgramData/...`。
6. **改了 conda 的 LLVM/依赖 cmake 文件**：必须删除/重建 build 缓存或重新 configure，否则旧值还在 cache 里。
7. **改 conda 环境文件先备份**（`.bak.envfix`），便于回滚；这事改的是**台式机环境**，不是仓库源码，符合"尽量不改项目"的约束。

---

## 七、本轮完成的工作成果

| 项 | 结果 |
|----|------|
| 修复台式机 WSL 卡死 | ✅ 升级 Store 版 WSL 2.7.14，绕过 LxssManager 死锁 |
| 同步笔记本最新源码到台式机 | ✅ 解压到 `/opt/plc2/plc-inauto-compiler` |
| 清理 fmt 污染 | ✅ 保留唯一 fmt10 |
| LLVM CommandLine 重复注册 | ✅ 改环境 `LLVM-Config.cmake` 注入 dylib 短路，**项目源码零改动** |
| `plc-compiler` 编译 | ✅ 成功，运行正常产出 LLVM IR |
| 集成测试 | ✅ 1767 tests / 116 套件，PASSED 1581、FAILED 0 |
| 台式机编译耗时 | ✅ 全流程（configure+build+测试）约 66s，远快于笔记本 |

---

## 八、后续建议

1. 若需长期在台式机构建，建议把 2.2 的「编码→scp→解码执行」封装成一个本机 PowerShell 函数/脚本复用。
2. LLVM 下的 `*.bak.envfix` 备份建议保留，确认稳定后由台式机管理员决定是否清理。
3. 台式机无外网，任何新工具/依赖必须先在本机下载、用 base64/scp 传过去。